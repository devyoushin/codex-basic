# AGENTS.md — codex-basic 작업 규칙 (Codex 전용)

이 디렉터리는 **Codex CLI 사용법을 Codex로 정리하는 레포**입니다. 이 트리 안에서 작업할 때 아래 규칙을 우선 적용합니다.

## 1) 기본 작업 흐름 (필수)

1. **요구사항 재확인**: “무엇을 만들지/바꿀지”를 1~2문장으로 정리
2. **탐색은 빠르게**: 파일/텍스트 검색은 `rg` 우선
3. **변경은 최소로**: 필요한 파일만, 작은 패치로, 목적 외 리팩터링 금지
4. **검증**: 가능한 가장 좁은 테스트/명령부터 실행
5. **정리**: 결과/사용법을 문서에 반영(필요 시 템플릿/규칙 업데이트)

## 2) 도구 사용 원칙

- 파일 수정은 **`apply_patch`** 우선(실수/충돌 최소화).
- 여러 파일 읽기/검색은 가능하면 **병렬**로 수행(빠른 컨텍스트 수집).
- 명령은 “읽기(검색/조회) → 좁은 검증 → 넓은 검증” 순으로 실행.

## 3) 안전/권한

- 파괴적 명령(`rm`, `git reset --hard`, 대량 삭제 등)은 사용자 요청 없이는 실행 금지.
- 네트워크/외부 접근이 필요한 명령은 승인(에스컬레이션)이 필요할 수 있음.
- 비밀/토큰/자격 증명은 파일에 남기지 않음(예시는 `<YOUR_VALUE>`).

## 4) 커밋 메시지 규칙 (AI 사용 표기)

AI 도움을 받았으면 커밋 메시지 **본문 하단 트레일러**에 아래 형식으로 남깁니다.

```text
<subject>

AI-Assistant: Codex
```

혼용이면 `AI-Assistant: Claude, Codex`처럼 콤마로 병기.

## 5) 문서 스타일(이 레포 내부)

- 기본 언어: 한국어
- 불필요한 서론/미사여구 금지(짧고 직접)
- 코드/명령은 항상 코드블록 + 언어 태그
- 체크리스트/규칙은 번호 또는 불릿으로 간결히

참조:
- `docs/rules/` 전체
- `docs/templates/` 템플릿

