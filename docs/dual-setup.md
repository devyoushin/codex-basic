# Claude + Codex 공존 세팅 가이드

## 목표

한 레포에서 “콘텐츠(문서/코드)”는 공용으로 두고, “에이전트별 진입점”만 분리해 운영합니다.

## 추천 구조

- 공용: `docs/`, `templates/`, `rules/`(문서 스타일/네이밍/보안 등)
- Claude 진입점: `CLAUDE.md` (+ 필요 시 `.claude/`)
- Codex 진입점: `AGENTS.md` (폴더별로 더 세분화 가능)

핵심은 “규칙을 한 곳에 모으고, 각 에이전트 진입점에서 그 규칙을 참조”하는 방식입니다.

## 커밋 메시지로 누가 했는지 남기기

도구별로 커밋 메시지 본문 하단에 남깁니다:

```text
AI-Assistant: Claude
```

```text
AI-Assistant: Codex
```

이렇게 해두면 나중에 “같은 레포를 Claude로 다시 열었을 때도” 어떤 커밋이 어떤 도구 도움으로 만들어졌는지 추적이 쉽습니다.

